package com.example.tms.route;

public class RoutePlanner {
	public Route findOptimalRoute(Location start, Location end, Vehicle vehicle) {
        // Mock implementation for simplicity
        Route route = new Route();
        route.setDistance(100); // Example distance
        route.setDuration(120); // Example duration in minutes
        return route;
    }
}